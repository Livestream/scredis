package scredis.protocol.commands

import scredis.protocol._

import java.nio.ByteBuffer

/*
case class Auth(password: String) extends Command[Unit] {
  val name = "PING"
  val encoded = encode(Seq(password))
  val decoder = null
}

object Ping extends Command[String] {
  val name = "PING"
  val encoded = encode()
  val decoder = null
}*/
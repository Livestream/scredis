unauthenticated.scredis.client.port = 6380
authenticated.scredis.client {
  port = 6380
  password = foobar
}

akka {
  loglevel = "INFO"
  log-dead-letters = off
  
  actor {
    debug {
      # enable function of LoggingReceive, which is to log any received message at
      # DEBUG level
      receive = off
    }
  }
  
}